#+title: Next session
#+date: 2026-08-05
#+filetags: :IODefl:session:pointer:

This file is OVERWRITTEN at the end of every session, never appended.
It describes one session: the next one.
History belongs to git, to the issue tracker, and to =docs/superpowers/handoffs/=.

Previous session (2026-08-05, third of the day, on the machine without the data):
closed #25 and #29, derived #26 without closing it, and filed #42.
*The next session is expected on the machine that holds the data*, and the full
handoff for it is
=docs/superpowers/handoffs/2026-08-05-data-machine.org= — read that first.

* FIRST: find out where you are

Three locations now.

: test -f etc/Modelo/NIOTs_resultados.rdata && echo "DATA PRESENT" || echo "NO DATA"
: test -d /HDD/Model_OCDE && echo "SOURCE FOLDER PRESENT" || echo "NO SOURCE FOLDER"
: test -d ~/Documents/GrowthDecomposition && echo "NEW REPO PRESENT" || echo "NO NEW REPO"

- *DATA PRESENT* — the primary machine. Follow
  =docs/superpowers/handoffs/2026-08-05-data-machine.org=, which orders the four
  tickets that run there. Claim *#26* and run its demonstration first: it is two
  commands and it unblocks #27, #28 and #42.
- *NO DATA + SOURCE + NEW REPO* — the second machine. #26 cannot be finished
  there. Claim *#38* or *#39*, both of which need the new repository and not the
  data, or *#30*, *#31*, *#32* on map #19.

Do not infer any answer from the hostname or from what a previous session said.
The third test is recent and the two folders it names exist on the second machine
only.

* Ticket to claim, on the primary machine

*#26, B2: derive the Stage-B input contract from the consuming code*
https://github.com/gpetrini/Deflating_WIOD_Tables/issues/26

Already assigned. *The derivation is done and committed; only the demonstration
remains*, and the ticket does not close without it.

: setwd("code")
: load("../etc/Modelo/NIOTs_resultados.rdata")
: source("validate_niots_contract.R")
: validate_niots(NIOTs, strict = FALSE)   # must PASS
: validate_niots(NIOTs, strict = TRUE)    # expected to FAIL on the [0,1] check

Run the permissive call first: it checks only what would raise an error and must
pass. If it does not, the contract document is wrong about the real object, and
that is the finding rather than a validator bug.

=strict = TRUE= is *expected to fail* on the import-share check, because #34
records 81 of 11700 cells outside =[0,1]= for =E= (INVNT).
Record that failure as the expected reproduction of #34, quoting the country and
year the validator names — a datum #34 does not yet have.

Close #26 with both outputs pasted in.

** After #26, on that machine

*#34* (unblocked by #25; #35 waits behind it), then *#36*, then *#40*.
Costs measured on that machine: full reconstruction 27 seconds; one group's
figures about a minute; *the full driver 66 minutes for 77 groups*.

* Recommended model

*Opus 5* for #34, which decides a treatment.
A cheaper model suffices for the #26 demonstration, which is two commands and a
transcription.

* Ideal opening prompt

#+begin_example
Continuar o mapa #18, agora na máquina COM os dados. Rode os três testes do
NEXT_SESSION e leia
docs/superpowers/handoffs/2026-08-05-data-machine.org antes de escolher ticket.

Comece pelo #26, que só falta a demonstração do validador contra o NIOTs real:
rodar strict = FALSE primeiro (deve passar) e strict = TRUE depois (deve falhar
no teste [0,1], reproduzindo o #34). Feche o #26 com as duas saídas.

Depois #34, #36 e #40, nessa ordem.

git pull primeiro: seis commits foram empurrados em 2026-08-05, até 496b33f.
Para empurrar daqui é preciso
git -c credential.helper='!gh auth git-credential' push origin reporting/refinements
e o gh precisa de -R gpetrini/Deflating_WIOD_Tables neste checkout.
#+end_example

* Context this session needs

- =docs/superpowers/handoffs/2026-08-05-data-machine.org= — o handoff completo.
- =docs/superpowers/specs/2026-08-05-stage-b-input-contract.org= — o contrato do
  #26, e o que o validador deliberadamente não testa.
- =docs/superpowers/findings/2026-08-05-2025-vintage-and-the-niots-path.org= — o
  inventário do #25.
- =docs/superpowers/specs/2026-08-05-new-repository-created.org= — o que o #29
  construiu, e a correção sobre =country_groups=.
- Mapa #18.

* State of the working tree

Clean on the second machine.
Branch =reporting/refinements=, *pushed and in sync with origin at =496b33f===.
The primary machine needs =git pull=; its own =figs/=, =tabs/= and =objs/= are
git-ignored and will not appear in =git status=.

Three stray files in =tabs/= from an exploratory call to
=tabulate_period_means()=, left in place by author decision.

* Board state

Frontier means open, unblocked and unclaimed.

| Map                      | Frontier                                                   | Runs where |
|--------------------------+------------------------------------------------------------+------------|
| #18 Zenodo-backed repo   | *#26* demonstration only; *#34*; *#38*, *#39*               | #26, #34 need the data; #38, #39 need the new repo |
|                          | #27 blocked by #26; #28 by #27; #35 by #34; #42 by #28      |            |
| #19 retire and inventory | *#30*, *#31*, *#32*; #33 blocked by #32                     | anywhere   |
| (no map)                 | *#36*, *#40*                                               | the data   |

Closed on 2026-08-05: #20 to #24 (map #17), then #37, #41, #25 and #29 on map #18.
Opened: #42.

** Maps to open later

- A *manuscript map* becomes chartable once #36 reports.
- A *Stage-A map*, now well motivated: #25 established that the 2025 vintage has
  no route to a =NIOTs= object, that its geometry is 50 sectors rather than 45,
  and that the only working builder is the legacy one in =archive/= on the second
  machine.

* Everything else

Not repeated here.
The facts carried forward, the traps of running the driver, the tracker mechanics,
and what waits on the author and the co-author are all in
=docs/superpowers/handoffs/2026-08-05-data-machine.org=.
