#+title: Next session
#+date: 2026-08-05
#+filetags: :IODefl:session:pointer:

This file is OVERWRITTEN at the end of every session, never appended.
It describes one session: the next one.
History belongs to git, to the issue tracker, and to =docs/superpowers/handoffs/=.

Previous session (2026-08-05): closed map #17 entire, resolving #21, #22, #23 and
#24; opened #34, #35, #36 and #40; revised map #18 from the Syncthing arrangement
to the Zenodo arrangement and opened #37, #38 and #39.

* If you are the agent on the second machine

Read =docs/superpowers/handoffs/2026-08-05-other-machine.org= first.
It covers what is specific to working without the data on disk, and then sends
you back here.

* The machine decides the ticket

The author works part of the time on a machine that does not hold
=NIOTs_resultados.rdata=.
Tickets that run there carry the *=no-data-required=* label on the tracker:

: gh issue list --state open --label no-data-required

Everything else needs the data and must wait for the machine that has it.

* Ticket to claim, on a machine WITHOUT the data

*#37, B6: design the Zenodo deposit and its file manifest*
https://github.com/gpetrini/Deflating_WIOD_Tables/issues/37
Map: #18, migrate Stage-B reporting to a new Zenodo-backed repository.

Claim it first: =gh issue edit 37 --add-assignee gpetrini=.

*#29 is no longer available there.* The author settled on 2026-08-05 that the
layout inherits from an existing project folder, to be downloaded from the cloud
to the machine that holds the data.
That made the layout an inheritance rather than a free decision, so #29 is now
blocked by #41 and lost its =no-data-required= label.
Since #38 and #39 are blocked by #29, the whole rest of the chain waits on #41.

#37 is unblocked and independent of all of it.
If it finishes, map #19 has three data-free tickets: #30, #31 and #32.
Ticket #26 is also data-free to write, with its validator demonstration left
outstanding.

* Ticket to claim, on the machine WITH the data

*Map #17 is CLOSED.* #24 resolved on 2026-08-05 and the map reached its
destination; #34 and #35 were reparented to map #18.

Two things wait on this machine.

*#40, verify the figure republication.*
https://github.com/gpetrini/Deflating_WIOD_Tables/issues/40
A full rerun was launched detached at 13:47 on 2026-08-05 and the session ended
before it finished.
Until it is verified =figs/= is in a mixed state, since the Developed group was
regenerated separately during the decision work.
Cheap: read a log, sample two figures.

*#36, the cross-method sign disagreement*, which carries its own measured
evidence in the body and needs no re-derivation.

* Recommended model

*Opus 5* for #37 and, on the other machine, #41.
The deposit decision constrains what a stranger can reproduce, and #41 has to
judge which inherited layout choices conflict with constraints this map already
recorded.
#38 and #39 are largely specified and suit a cheaper model once #29 lands.

* Ideal opening prompt

#+begin_example
Continuar o wayfinder map #18, que foi revisado em 2026-08-05: o destino agora é
um repositório git novo, criado do zero, com os dados no Zenodo e um Makefile
como ponto de entrada único. Syncthing saiu de cena.

Assumir o ticket #37: desenhar o depósito Zenodo e seu manifesto.

Estou numa máquina SEM os dados em disco, então só tickets com o rótulo
no-data-required. NÃO pegar o #29, que passou a depender do #41.

Decidir o que vai no depósito, se o release externo bruto viaja junto, a
serialização, o versionamento, a licença e os checksums que o Makefile do #38
vai conferir. O corpo do ticket já traz os tamanhos dos dois candidatos a objeto
de fronteira, que diferem em uma ordem de grandeza.

gh resolve o repositório pelo remote, então rodar de dentro do checkout ou usar
-R gpetrini/Deflating_WIOD_Tables.
#+end_example

* Context this session needs

- Map #18 body, reescrito em 2026-08-05; a seção Notes registra o que mudou e por quê.
- Ticket #41, que define de onde o layout é herdado.
- =docs/adr/=, doze registros, que o #39 vai triar.
- Este repositório =.gitignore=, como exemplo do que não repetir.

* State of the working tree

Clean.
Branch =reporting/refinements=, *pushed* and tracking
=origin/reporting/refinements=.
It branched off =diagnosis/xr-gap-debug=; =master= is far behind both, and whether
this work reaches =master= is still undecided.

Three stray files in =tabs/= from an exploratory call to =tabulate_period_means()=,
left in place by author decision on 2026-08-05: =Period_Means_Attribution.tex=,
=Period_Means_AverageImportContent.tex=, =Period_Means_NetExports.tex=.
They are not a deliverable of any ticket.

* Facts carried forward

- *Map #18 was revised, not merely extended.*
  The Syncthing arrangement is recorded under its Out of scope section.
  Ticket #29 was rewritten; do not read its original body as current.
- *Neither the target repository nor the Zenodo deposit exists yet.*
  The Zenodo record identifier is a parameter until the deposit is made, and
  =make data= cannot be tested end to end before then.
- *=Super Decomposição= is still the source of the new data treatment* (#25) and
  is no longer the target repository.
- *Two copies of the boundary object differ by an order of magnitude*:
  =etc/Modelo/NIOTs_resultados.rdata= is 8.7 MB, =results/NIOTs_resultados.rdata=
  is 88 MB. Which one is the boundary object is an open question in #37.
- *=group_plots= writes =../reports/<group>.pdf= directly* and fails if the
  directory is absent, which is why =make dirs= exists.
- *Rebuild costs, measured on 2026-08-05.*
  Reconstructing everything from scratch takes *27 seconds*: 1.9s to source, 17.8s
  for =prepare_data()=, 3.9s for =decompose_growth()=, 3.4s for the period-means
  table.
  Regenerating one group's figures adds about a minute.
  The expensive act is the full driver, 66 minutes for 77 groups in two formats,
  roughly 51 seconds per group.
  *Do not confuse the two.* Verifying a code change costs half a minute; only a
  deliberate republication of the whole figure set costs an hour, and that is not
  a requirement of #24.
- *Tier-B harness:* =code/verify_reporting_refinements.R=.
  The =decomp= fingerprint is unchanged at tolerance zero across every ticket
  closed on map #17, since none of them moved a number.
- *The plan code is unrun and has defects.*
  Two were corrected on 2026-08-05: a =get0()= default resolving to its own
  promise, and a zero-growth guard three orders of magnitude too tight.
- *The E (INVNT) import share leaves [0,1]* in 81 of 11700 cells.
  Deferred as #34 behind #25: it may already be fixed upstream.
  It is a live constraint on any number the new repository reports, which is why
  #39 carries the finding forward.
- *=tabulate_period_means()= writes to =../tabs/= on every call*, so exploratory
  use contaminates published output. Redirect its =tabs= argument.
- *The =figs= argument of every plotting function is dead.*
  No =save_figs()= call passes it, so the argument is accepted and ignored and
  output always lands in =../figs/=.
  An attempt to render into a scratch directory silently overwrote the published
  set instead. Filed on #32.
- *The gap diagnosis is resolved upstream.* Do not run the archived diagnosis
  scripts.
- *Tracker mechanics.* Sub-issues:
  =gh api repos/:owner/:repo/issues/N/sub_issues -F sub_issue_id=<numeric id>=.
  Blocking: =.../issues/N/dependencies/blocked_by -F issue_id=<numeric id>=.
  Use =-F=, not =-f=.
  =gh issue view= fails here with a Projects-classic deprecation error; read
  bodies with =gh api repos/:owner/:repo/issues/N --jq '.body'=.
- *The repository =.gitignore= excludes Markdown.* Write documents in Org.

* Nothing waiting on the user

Two decisions were taken with the author on 2026-08-05: the zero-growth threshold
at 0.5 p.p., and the target being a new repository with a Zenodo deposit, neither
of which exists yet.

* Board state

| Map                       | Frontier: open, unblocked, unclaimed                          |
|---------------------------+---------------------------------------------------------------|
| #17 reporting refinements | *CLOSED 2026-08-05*; #34 and #35 moved to map #18            |
| #18 Zenodo-backed repo    | *#29*, *#37* data-free; #25, #26 (#26 data-free to write);    |
|                           | #34 and #35 inherited, both blocked behind #25;               |
|                           | *#41* gates #29, which gates #38 and #39;                     |
|                           | #38 blocked by #29 and #37; #39 by #29; #27 by #25 and #26;   |
|                           | #28 by #27                                                    |
| #19 retire and inventory  | #30, #31, #32 all data-free; #33 blocked by #32               |
| (no map)                  | #36 sign disagreement, #40 verify the republication; both     |
|                           | need the data                                                 |

** Maps to open later

- *Nothing new to chart yet.* Map #18 absorbed the Zenodo and Makefile work,
  which is where it belongs: the destination is a repository a stranger can
  clone and run.
- A *manuscript map* becomes chartable once #36 reports, since the
  sign-disagreement result is a paper section rather than a code deliverable.
- A *Stage-A map* would be needed only if #37 decides the deposit carries the
  raw external release, which puts the deflation stage back in reproduction scope.
